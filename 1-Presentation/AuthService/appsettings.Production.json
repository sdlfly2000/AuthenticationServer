{
  "ConnectionStrings": {
    "IdentityDatabase": "server={{dbHost}};database=Identity;uid={{dbUsername}};password={{dbPassword}};TrustServerCertificate=true",
    "Redis": "{{redisHost}},password={{redisPassword}}"
  },

  "RabbitMQ": {
    "Host": "{{rabbitMqHost}}"
  }
}
